import { Box, Typography } from "@mui/material";

const CodeRoute = () => {
  return (
    <Box component="div" className="route-box">
      <Typography>This will be a Code route.</Typography>
    </Box>
  );
};

export default CodeRoute;
